import { HtmlRemovePipe } from './html-remove.pipe';

describe('HtmlRemovePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlRemovePipe();
    expect(pipe).toBeTruthy();
  });
});

import { ClassSetterDirective } from './class-setter.directive';

describe('ClassSetterDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassSetterDirective();
    expect(directive).toBeTruthy();
  });
});

import { AttributeSetterDirective } from './attribute-setter.directive';

describe('AttributeSetterDirective', () => {
  it('should create an instance', () => {
    // const directive = new AttributeSetterDirective();
    // expect(directive).toBeTruthy();
  });
});

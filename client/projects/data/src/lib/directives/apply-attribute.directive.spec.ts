import { ApplyAttributeDirective } from './apply-attribute.directive';

describe('ApplyAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyAttributeDirective();
    expect(directive).toBeTruthy();
  });
});

import { FlyoutTriggerClassSetterDirective } from './flyout-trigger-class-setter.directive';

describe('FlyoutTriggerClassSetterDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyoutTriggerClassSetterDirective();
    expect(directive).toBeTruthy();
  });
});

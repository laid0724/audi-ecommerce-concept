import { ProjectAsFieldDirective } from './project-as-field.directive';

describe('ProjectAsFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectAsFieldDirective();
    expect(directive).toBeTruthy();
  });
});

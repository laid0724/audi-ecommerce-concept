import { ProjectAsFieldsDirective } from './project-as-fields.directive';

describe('ProjectAsFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectAsFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
